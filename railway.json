{
    "build": {
        "command": "composer install --no-dev && php artisan optimize && php artisan storage:link"
    },
    "deploy": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}",
        "healthcheckPath": "/",
        "restartPolicyType": "never"
    }
}
